# 开发/测试依赖（非生产运行所需）
# 安装：pip install -r src/tests/requirements-dev.txt

# 测试框架（src/tests/ 全部测试文件使用；见 src/tests/README.md 获取清单）
pytest>=7.0.0

# 测试覆盖率报告（可选，src/tests/run_tests.bat --cov 使用）
pytest-cov>=4.0.0

# HTTP 请求库（多处测试使用，如 test_tmdb_api.py、test_openlist_admin_api.py）
requests>=2.31.0

# Mock TMDB API 服务器（独立调试工具，非 pytest 用例；仅 src/tests/test_tmdb_api.py --flask 启动）
flask>=2.0.0

# 字体测试依赖（仅用于字体子集化、WOFF2 cmap 读取及发布字体覆盖测试；
# 见 src/tests/test_subset_font.py。不是生产运行依赖，仅开发/测试所需。）
fonttools[woff2]>=4.50.0

