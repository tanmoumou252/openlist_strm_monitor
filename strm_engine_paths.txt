# ============================================================
# STRM 引擎管辖区（安全白名单） - strm_engine_paths.txt
# ============================================================
#
# 【填什么？】
# 这里填写你在 OpenList 中为 STRM 驱动设置的“挂载路径 (mount_path)”。
# 以 / 开头，不要以 / 结尾。
#
# 【为什么填这个？】
# 这是程序的“防误删安全底线”。
# 程序只有明确知道这个路径归 STRM 引擎管，才敢在里面执行“清理已失效的死记录”。
# 如果不属于这里的路径，程序为了保护你的数据，会拒绝清理任何冗余文件。
#
# 示例：
# /strm
# /测试a
#
# ============================================================
/测试a