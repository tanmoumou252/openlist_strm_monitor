# 否决方案.md — 审计排除登记表

> **用途**：单文件权威登记，供后续 agent 避免重扫已否决/已证伪/已接受项。
> 本文件只登记未修复的项。已修复的缺陷记录在 `.kilo/plans/` 中，不在此登记。

---

## 一、审计排除项

| 类别 | 事项 | 代码位置 | 理由/结论 |
|------|------|----------|-----------|
| 设计决策 | copyfile 锁外恢复 | `app_service_core.py` | 避免死锁，恢复在锁外尽力执行 |
| 设计决策 | `_safe_call` 吞异常 | `area_watchers.py` | 杀不死 watchdog，保持监视器存活 |
| 设计决策 | `anime_max_season_diff` 保留 | `AppConfig` | 硬编码 `+1`，保留字段供未来扩展 |
| 设计决策 | `BEGIN IMMEDIATE` 探针 | `database.py` | 与模式一致，保留 |
| 设计决策 | `secret_manager` 无 HMAC | `secret_manager.py` | 本地文件系统已为信任边界，HMAC 过度设计 |
| 设计决策 | `tmdb_watchlist_db` meta 回退 | `tmdb_watchlist_db.py` | 读取静默回退，避免启动失败 |
| 设计决策 | `sync_service` 尽力回滚 | `app_service_core.py` | 回滚忽略返回值，尽力清理 |
| 设计决策 | `webdav_utils` 双语义 | `webdav_utils.py` | 设计分工，勿合并 |
| 设计决策 | `strm_utils` LIKE 转义 | `strm_utils.py` | SQLite 无 `[]` 语法，保留 |
| 设计决策 | 白名单 GET-only | `server.py` | SPA onboarding 需求，保留 |
| 设计决策 | config 布尔字段语义 | `config.py` | 配置布尔字段语义，保留 |
| 设计决策 | DB 固定项目根 | `config.py` | 用户决策固定项目根，保留 |
| 设计决策 | `config.toml` 最小模板 | `config.toml.example` | WebUI 为主程序，最小模板正确 |
| 设计决策 | B 区 100 页上限安全阀 | `app_service_core.py` | 大目录整组跳过，不造成数据损坏 |
| 设计决策 | FTS5 引号包裹 | `routes.py` `_escape_fts5_query` | 提交 1ab6826 有意引入，使 FTS5 按短语精确匹配 |
| 观察项 | `_prune_tmdb_logs` LIMIT 0 语义（C4） | `tmdb_watchlist_db.py` `_prune_tmdb_logs` | 默认 `_tmdb_log_max_rows=1000` 不触发，但 `LIMIT 0` → `NOT IN (empty)` 为 vacuously true → 清空全表。已在 C4 修复：`max(1, rows)` 下限保护，此登记保留供追溯 |
| 设计决策 | test-connection 忽略请求体 host | `routes.py` | 仅用配置 host，防 SSRF |
| 设计决策 | `_read_lock` 死代码移除 | `database.py` | 行级隔离由 SQLite WAL 快照提供 |
| 设计决策 | 按 mapping_id 分区聚合 | `watchlist_match.py` `collect_b_media_snapshot` | 避免多根同名媒体合并 |
| 设计决策 | 畸形 JSON 限流 | `routes.py` login | 恶意 JSON 计入限流，与合法非 dict 不计语义区分 |
| 非缺陷 | 幽灵文档 `docs/` 某处 | `docs/` | 已核实为历史遗留，非当前代码路径 |
| 非缺陷 | 幽灵文档 `wiki/` 某处 | `wiki/` | 已核实为历史遗留，非当前代码路径 |
| 非缺陷 | `reset_admin.py` 密码含连字符 | `reset_admin.py` | 密码生成逻辑正确，连字符为合法字符 |
| 非缺陷 | `monitored_paths=None` | `config.py` | 空列表合法，None 由调用方处理 |
| 非缺陷 | 合并审计误判 | 多文件 | 已核实为跨文件误关联，非真实缺陷 |
| 非缺陷 | wiki 索引卡字段标注 | `wiki/WebUI-Frontend.md` | wiki 描述正确，agent 误判 |
| 非缺陷 | `环境变量启动.bat` `where` 完整路径 | `环境变量启动.bat` | `where` 对绝对路径返回 0，后续版本检查兜底 |
| 非缺陷 | `watchlist_db` 硬编码 | `routes.py` | 提交 6c3e489 有意移除该配置项 |
| 非缺陷 | watchlist `_status`/`_is_manual` 写入 | `routes.py` | `get_all()` 返回新副本，写入不影响缓存 |
| 非缺陷 | CDN 重定向无 CRLF 头注入 | `routes.py` | HTTP 解析器已在请求行切分 CRLF |
| 非缺陷 | `%2e%2e` 编码穿越 | `routes.py` | `urlparse` 不做百分号解码，`..` 黑名单 + `resolve` 有效 |
| 非缺陷 | 区域搜索 SQL 注入 | `routes.py` | 表名/字段取自白名单，值均参数化 |
| 非缺陷 | `ReadWriteLock` 丢唤醒/死锁 | `database.py` | 无丢唤醒/死锁，`BEGIN IMMEDIATE` 嵌套合法 |
| 非缺陷 | `_is_lan_ip` 对非 LAN | `routes.py` | fail-closed，误判方向安全 |
| 非缺陷 | `docs/ink-reveal/` 水墨晕染特效已集成 | `src/webui/modules/core/wallpaper.js` | `wallpaper.js` 是 `ink-reveal.js` 的 ES module 生产实现，已在 WebUI 中正式启用（`main.js` 的 `initWallpaperReveal()` 调用）。README 文档导航中的链接正确，不应标注为"预留未集成" |
| 已否决 | 不重构同步引擎架构 | — | 接近完成，重构风险高 |
| 已否决 | 不修改数据库结构 | — | 生产环境稳定，结构变更需迁移 |
| 已否决 | 不修改 `check_exists()` 三态语义 | — | fail-closed 设计，变更破坏安全模型 |
| 已否决 | 不扩大 API 行为边界 | — | 当前边界已明确，扩大引入兼容风险 |
| 已否决 | 不重构 TMDB API 代码 | — | 除非具体请求，当前实现稳定 |
| 已否决 | 不删除 `anime_min_season_ratio` 字段 | — | 保留待用，已加 WebUI tooltip |
| 已否决 | 不修改 `_login_attempts` 数据结构 | — | 时间戳列表为正确设计，支持滑动窗口 |
| 已否决 | 不修改 `_do_bg_sync` 预检查逻辑 | — | 已加固，显式检查 `_tmdb_client is None` |
| 已否决 | 不修改 `_csv_safe_text` 公式前缀 | — | `=+-@` 为电子表格标准公式前缀 |
| 已否决 | 不修改 dialog `htmlContent` 运行时守卫 | — | 已升级为运行时守卫（catch XSS 向量），不再只是 console.assert；见 `dialog.js` `showConfirmDialog` |
| 已否决 | 不修改 `delete_a_batch`/`delete_b_batch` 返回值 | — | 当前无生产调用方，返回输入列表长度 |
| 设计决策 | `check_exists` 缓存竞态 | `webdav_client.py` `check_exists` | 重复请求概率低，`list_directory` 有重试和超时，加 per-key 锁增加复杂度，接受为性能取舍 |
| 设计决策 | `requests.Session()` 无 `close()` | `webdav_client.py` | Session 生命周期与进程一致，进程退出时 OS 回收资源，热重载产生的旧 Session 会被 GC 回收 |
| 设计决策 | `refresh_service.py` 审计逻辑重复 | `refresh_service.py` | 审计逻辑是运营关键路径，重构风险 > 收益，偏好小改动 |
| 设计决策 | GET 端点触发 TMDB 同步副作用 | `routes.py` | 改 GET→POST 会破坏现有前端/API 调用方，在文档中明确标注此端点的副作用即可 |
| 设计决策 | `_cn_to_int` 重复 3 份 | `config.py` / `watchlist_match.py` / `media_renamer.py` | 提取到共享工具虽然低风险，但当前 3 份实现完全独立且稳定，重构可能引入不必要的跨模块依赖，接受为代码重复 |
| 设计决策 | `collect_b_media_snapshot` 跳过 `read_locked()` | `watchlist_match.py` `collect_b_media_snapshot` | WAL 提供行级快照隔离，跳过 `ReadWriteLock` 不影响读安全；低频调用，不会造成写者饥饿 |
| 设计决策 | 900 保守切片 `IN(...)` 预读 | `utils/file_utils.py` `chunk_list` / `database.py` `upsert_a_batch`、`upsert_b_batch`、`cleanup_invalid_subtitles` / `domain/sync/sync_service.py` `_upsert_a_batch_bulk` / `tmdb_watchlist_db.py` `_upsert_movies_batch`、`_upsert_tvs_batch` | 现代 SQLite（≥3.32，默认 32766）下 1000 参数不崩溃；但嵌入式/旧版 SQLite 的 `SQLITE_MAX_VARIABLE_NUMBER` 仍为 999。900 切片是跨版本防御，且让"每 1000 条一次提交"的批提交语义与预读切片解耦。勿以"现代 SQLite 上限 32766、切片多余"为由删除；也不要把切片数改成 ≥1000 |
| 已知取舍 | `AppConfig.from_file` 使用 `__new__` | `config.py` `AppConfig.from_file` | 绕过 `__init__`/`__post_init__`，当前无 `__post_init__` 验证逻辑，风险低 |
| 已知取舍 | `tmdb_client.py` 每次请求重建 `opener` | `tmdb_client.py` `urlopen` 路径 | 性能影响小，仅批量操作时可感知，保留当前实现 |
| 已知取舍 | `_path_locks` 超 10000 仅警告不清理 | `app_service_core.py` `_path_locks` | 已加警告日志，长期运行才可能内存累积，暂不引入 LRU |
| 已知取舍 | `move_file` 跨设备回退用 `os.remove` | `utils/file_utils.py` `move_file` | B 区移动不涉及跨设备场景，风险实际不可达 |
| 已知取舍 | `reset_admin.py` 绕过 DB 抽象层 | `reset_admin.py` | 独立维护 CLI 工具，保持自包含（不依赖 `database.py` 的 `Database` 类），降低部署耦合 |
| 已知取舍 | `_setUiConfig` 乐观更新竞态 | `state.js` `_setUiConfig` | 已有 AbortController + 版本号防护，快速连续点击时旧响应不会覆盖新状态 |
| 设计决策 | `touch_verified_b` 跨 mapping 无 `mapping_id` 过滤（O-1） | `database.py` `touch_verified_b` | 仅用于全量审计后批量标记，调用方保证 mapping 隔离，加 mapping_id 过滤增加复杂度且无实际收益 |
| 设计决策 | `os.access` 异常未捕获（O-2） | `logger_setup.py` | `os.access` 在 Windows 上可能抛出 `OSError`（权限/NTFS 流），当前设计为"尽力检测"，exception 会冒泡到调用方，接受为已知边界 |
| 已知取舍 | `logs.js` 残留 `N0/T9` 考古编号注释（Q1-2） | `modules/pages/logs.js` `_fetchAndRenderLogs` | 第 17 轮清理后唯一残留的前缀编号注释，属代码逻辑说明而非计划服务注释，已清理（仅 71 行残留）；历史状态登记为已知取舍，后续 agent 扫描时如已清理则跳过，如未清理则按约定剥离前缀保留解释 |
| 设计决策 | `# 设计决策：` 共 3 处，`# 有意保留：` 共 2 处（合计 5 处） | `database.py`（`move_b_record`）、`sync_service.py`（`_sync_one_record`）、`routes.py`（`handle_login`）为设计决策；`tmdb_watchlist_db.py`（单元测试 helper 方法）为有意保留 | 之前各计划中"9 处"为 `设计决策|有意保留|已修复` 过宽 grep 的结果，误将测试文件中的散文文本（如 `test_integration.py` "验证已修复的问题"）计入。以此 5 处为准确计数。grep 时应使用 `# 设计决策：` 或 `# 有意保留：` 精确匹配，避免泛匹配 |
| 已否决 | 不实现深色模式 | `src/webui/styles/main.css` | 本项目仅做浅色主题。深色主题会显著增加 CSS 变量复杂度(每个颜色/表面变量需要深色版本),且当前 WebUI 的壁纸系统(水墨晕染遮罩)和半透明表面设计以浅色背景为基准。用户明确只需浅色,深色为过度设计 |
| 设计决策 | `_handle_new_b_file` 不查 ghost_protected | `app_service_core.py` `_handle_new_b_file` | B 区手动添加场景，与 A→B 同步语义不同；前有 lineage 和 A 源双重校验 |
| 设计决策 | `migrate_b_under_root_to_c` 不设 ghost | `app_service_core.py` `migrate_b_under_root_to_c` | C 区幽灵保留历史痕迹，引擎重新添加后 B 区重建是预期行为 |
| 设计决策 | `ensure_single_visible_instance` watchdog 竞态 | `app_service_core.py` `ensure_single_visible_instance` | 有 B3-A/B3-B 回滚保护，不会造成 DB/磁盘分叉。**G-1 子注**：预标阶段 `mark_other_b_instances_duplicate` 一次性把所有兄弟实例置为 duplicate；若后续隔离循环中某实例「物理隔离 + 回滚均失败」而 raise（B3-B 路径），该兄弟之后的未处理实例会保持 DB=duplicate / 磁盘=.strm 的永久分叉，且 `valid_files` 过滤器（status='valid'）使本函数永不重试它们。此为已知取舍：触发窗口极窄（磁盘满/杀毒锁叠加），换取「物理隔离失败时逐个恢复 valid」的可重试语义。源码预标处已加 `# 已知取舍：` 注释，勿按普通缺陷修复 |
| 已移除 | `normalize_path` 死代码删除（C8） | `utils/file_utils.py` | 全仓库零调用，已从代码删除；登记防止后续 agent 报"缺失函数" |
| 已知取舍 | `login.js` 非 JSON 响应异常归并到网络错误（P3-1/P3-2 部分修复） | `modules/pages/login.js` | `resp.json()` 在外层 try，非 JSON 响应冒泡到外层 catch 显示"无法连接"。作用域场景（后端损坏/重启）极低，正确性影响有限 |
| 设计决策 | 仪表盘 `#0` 索引代次显示（Q2-4 部分修复） | `pages/dashboard.js` | `#0` 明确表示"第 0 代/尚未全量索引"，与 `—`（未配置）语义不同，有意保留 |
| 设计决策 | `stat-value-primary`/`stat-value-large` 保留（Q2-4） | `pages/dashboard.js` + `main.css` | 用于关键指标视觉层级（B-valid/索引代次高亮、TMDB/运行时间 18px），非未完成留白 |
| 非缺陷 | 区域搜索/分类 Tab 不重置页码（P2-1/P2-2） | `pages/area.js` | 假阳性。`area.js` 解析 URL 参数时 `parseInt(params.page) \|\| 1`，URL 缺 `page` 时默认回到第 1 页；搜索与 Tab 切换的链接本就不带 `page`，行为正确，勿按缺陷修复 |
| 已知取舍 | config.js TMDB token 输入框始终为空、需重输（P3-1） | `pages/config.js` | 有意的防误覆盖设计：`tokenValue = ''` + 仅当用户实际输入时才上传 `access_token`，避免把空值/过期预览截断误写成真实凭据。不实现掩码回显，登记防止再次被报缺陷 |
| 已否决 | `testwebui.bat` 不纳入登记/修改（M-A） | 项目根 `testwebui.bat` | 个人本地测试脚本，非生产路径，非 WebUI 启动链。历史上已有「保持原样」决定；本登记用于终止后续审计循环，勿再扫描/重报该文件 |
| 设计决策 | BAT 启动器不重定向 stdout/stderr（第 35 轮） | `环境变量启动.bat` / `嵌入式启动.bat` | 第 35 轮移除 `>>bridge_boot.log 2>&1`，使 server.py 控制台日志与交互菜单直接显示在 BAT 窗口。`后台带Bridge启动webui.vbs` 保留 `>strm_bridge_boot.log 2>&1`（隐藏窗口需落盘）。两个 BAT 文件语义不同：BAT 是可见控制台，VBS 是隐藏窗口，重定向策略不同是设计决策 |
| 已知取舍 | test-connection 双日志根因（第 35 轮） | `routes.py` / `refresh_service.py` | 后台自动登录成功 vs test-connection 独立 client 401 的双日志现象。根因为：后台自动登录先走 `refresh_service.py` 的 `_do_login()`（成功，INFO 日志），test-connection 用独立 `/api/openlist/test-connection` 端点绕过缓存（失败，WARNING/ERROR 日志）。不动逻辑，仅登记根因说明 |
| 设计决策 | 前端 `ol-log-path` 字段删除（第 35 轮） | `openlist.js` | 后端 `log_file` 是核心配置（`config.py`/`routes.py`/`server.py`/`main.py` 仍需它），前端只读展示字段已删除。不发送保存（R2 行为），删除字段后上传逻辑不变 |
| 设计决策 | 第 35 轮注释清理保留 `# 设计决策：`/`# 有意保留：`/`# 已知取舍：`/`# 已废弃：` 锚点（第 35 轮） | 多文件 | 第 35 轮清理了 `P0-/P1-/P2-/C-2/C-3/B3-A/M5/N0/R1/I-9/F-3/T11c/A'.` 等计划编号前缀。保留 `# 设计决策：`/`# 有意保留：`/`# 已知取舍：`/`# [已废弃]` 锚点，后续 agent 勿按普通注释清理 |
| 设计决策 | TMDB logo local-first（第 35 轮） | `tmdb.js` `_resolveTmdbLogoUrl` | 默认返回本地 SVG（`/assets/tmdb-logo-header.svg`/`/assets/tmdb-logo-card.svg`），不再默认 CDN。反代 host 配置后拼反代地址。保留 `<img>` 结构（避免内联 SVG 重复梯度 ID 风险），图片加载失败时 onerror 回退到同路径。CDN 常量已移除 |
| 设计决策 | 仪表盘 meta-compact 修饰类（第 35 轮） | `dashboard.js` / `main.css` | 6 个元信息 stat-card（TMDB/运行时间/索引代次/最近索引/映射版本/映射版本生成）加 `meta-compact` 类，值字号 18px（原 28px）。不拆独立 grid，随 `auto-fit` 自然同排 |


---

## 二、N0 架构约束（渲染护栏）

**`isRenderStale()` 这类模块级单变量渲染护栏无法表达"按调用方的代际"**，已废弃并改为 `captureRenderGuard()` 代际快照工厂（`router.js`）：

- 每个异步入口在首次 `await` / `.then()` 之前调用 `captureRenderGuard()`，得到只对自身生效的 stale 判定闭包。
- 后续 agent **勿再退回模块级单变量方案**——无论赋 `myGen`（恒 false，护栏空转）还是 `-1`（恒 true，整站白屏）都是错的。
- 标签切换/刷新不经 router、不推进 `_renderGen`，代际护栏无法拦截"切标签后旧响应回填"；`logs.js` 用模块级 `_logsGeneration` 单独处理。

---

## 三、注释关键词约定（稳定 grep 锚点）

> 自第 17 轮起，代码注释不再使用计划编号前缀（如 `[已修复] R7:`、`[设计取舍] #10:`、`T2:`、`B3-A:` 等），改为人类可读的**稳定关键词**，供未来 agent 双向定位（grep 关键词 + 读本登记册）。

| 关键词 | 含义 | 原对应标记 |
|--------|------|------|
| `# 设计决策：` | 这是有意的设计取舍，勿按缺陷"修复" | 原 `[设计取舍]` 标记、有意的安全阀/回滚/fail-safe 行为 |
| `# 有意保留：` | 看似死代码但故意保留的字段/分支 | `anime_max_season_diff` 等保留待用字段 |
| （无关键词的普通注释） | 纯行为描述 | 原 `[已修复] X` 与裸编号设计注释剥离前缀后 |

**grep 约定：** 扫描时 `grep "设计决策"` / `grep "有意保留"` 即可发现所有"有意为之"的位置，再对照本登记册查详情，避免误报。登记项按**文件:函数名**索引（而非计划编号）。

**补充说明（第 35 轮）：** `logs.js` 中的 `// N0/T9:` 编号前缀注释除 71 行外已清理为纯中文说明，与 §三 约定一致。后续 agent 若在 `logs.js` 中看到 `N0/T9:` 前缀，应按约定剥离前缀保留解释；若已无前缀则跳过。

---

## 四、使用说明

1. **新增审计发现时**：先查本文件第一节排除表，若已登记则跳过。
2. **标记为"设计决策"、"非缺陷"或"已否决"前**：在第一节表中补一条，含类别、代码位置与理由。
3. **已修复的缺陷**：不在此登记，记录在 `.kilo/plans/` 对应的计划文件中。
4. **本文件纳入 Git 追踪**，位于 `docs/否决方案.md`。